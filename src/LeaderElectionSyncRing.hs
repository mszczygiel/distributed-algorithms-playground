module LeaderElectionSyncRing
  ( nodeProgram
  )
where

import           Universum
-- import           Control.Monad.IO.Class

-- data NodeConfig = NodeConfig -- local address, neighbour address

nodeProgram :: (MonadIO f) => f ()
nodeProgram = return ()
